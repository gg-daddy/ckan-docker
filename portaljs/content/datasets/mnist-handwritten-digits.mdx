---
datasetId: "mnist-handwritten-digits"
title: "MNIST - Handwritten Digit Recognition"
description: "The MNIST dataset is the most famous benchmark dataset in machine learning. It consists of 70,000 grayscale images of handwritten digits (0-9), each 28x28 pixels."
author: "Yann LeCun"
lastUpdated: "2026-01-01"
features:
  showChart: false
  showTable: true
  showDownloadButton: true
  showResources: true
huggingFaceId: "ylecun/mnist"
colabNotebook: "mnist-handwritten-digits.ipynb"
---

# Overview

The MNIST dataset is the most famous benchmark dataset in machine learning. It consists of 70,000 grayscale images of handwritten digits (0-9), each 28x28 pixels.

Originally created by Yann LeCun and colleagues, MNIST has become the "Hello World" of computer vision and deep learning. It's widely used for benchmarking image classification algorithms.

**Key Features:**
- 70,000 grayscale images (60K train, 10K test)
- 28x28 pixel resolution
- 10 classes (digits 0-9)
- Extracted from NIST databases

## Dataset Information

| Property | Value |
|----------|-------|
| **Task** | Image Classification |
| **Modality** | Image |
| **Language** | N/A |
| **Size** | 70K images |
| **Downloads** | 79K+ monthly |
| **Format** | parquet |
| **License** | MIT |

## Quick Start

### Using Hugging Face Datasets Library

```python
from datasets import load_dataset

# Load the dataset
dataset = load_dataset("ylecun/mnist")

# Explore the dataset
print(dataset)
print(dataset["train"][0])
```

### Using Pandas

```python
import pandas as pd
from datasets import load_dataset

# Load and convert to pandas
dataset = load_dataset("ylecun/mnist")
df = dataset["train"].to_pandas()

# Basic exploration
print(df.head())
print(df.info())
```

## Data Visualization

### Digit Distribution

The chart below shows the distribution of digit classes (0-9):

<PlotlyBarChart
  data={{ values: [
    { category: "0", count: 6903 },
    { category: "1", count: 7877 },
    { category: "2", count: 6990 },
    { category: "3", count: 7141 },
    { category: "4", count: 6824 },
    { category: "5", count: 6313 },
    { category: "6", count: 6876 },
    { category: "7", count: 7293 },
    { category: "8", count: 6825 },
    { category: "9", count: 6958 }
  ]}}
  xAxis="category"
  yAxis="count"
  title="MNIST Digit Class Distribution"
/>

## Data Structure

The dataset contains the following splits and features. Refer to the [Hugging Face Dataset Card](https://huggingface.co/datasets/ylecun/mnist) for detailed schema information.

## Use Cases

This dataset is commonly used for:

- Computer vision model training
- Deep learning benchmarking
- Transfer learning experiments
- Neural architecture evaluation

## Citation

If you use this dataset in your research, please cite it appropriately. See the [dataset page](https://huggingface.co/datasets/ylecun/mnist) for the recommended citation format.

## Additional Resources

- [Hugging Face Dataset Page](https://huggingface.co/datasets/ylecun/mnist)
- [Dataset Viewer](https://huggingface.co/datasets/ylecun/mnist/viewer)
- [Download Options](https://huggingface.co/datasets/ylecun/mnist/tree/main)

---

*This dataset documentation is automatically generated from Hugging Face Hub metadata.*
